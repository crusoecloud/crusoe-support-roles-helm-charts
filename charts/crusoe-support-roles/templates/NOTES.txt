Thank you for installing {{ .Chart.Name }}!

Your release is named {{ .Release.Name }}.

The following ClusterRoles have been created:
  - crusoe-support-readonly (comprehensive read-only access, no logs)
  - crusoe-support-operator (read-only + operational capabilities, no logs)
  - crusoe-support-logs (pod and log access only, bind at namespace level)

{{- if .Values.roleBindings.enabled }}
{{- if .Values.roleBindings.bindings }}

ClusterRoleBindings (cluster-wide access):
{{- range .Values.roleBindings.bindings }}
  - {{ .roleName }}
{{- end }}
{{- else }}

No cluster role bindings have been created.
{{- end }}
{{- end }}

{{- if .Values.namespaceRoleBindings.enabled }}
{{- if .Values.namespaceRoleBindings.bindings }}

RoleBindings (namespace-scoped access):
{{- range .Values.namespaceRoleBindings.bindings }}
{{- $binding := . }}
{{- range .namespaces }}
  - {{ $binding.roleName }} in namespace {{ . }}
{{- end }}
{{- end }}
{{- else }}

No namespace-scoped role bindings have been created.
{{- end }}
{{- end }}

To configure bindings, edit your values.yaml file and run helm upgrade.

To view created resources:
  kubectl get clusterroles -l app.kubernetes.io/instance={{ .Release.Name }}
  kubectl get clusterrolebindings -l app.kubernetes.io/instance={{ .Release.Name }}
  kubectl get rolebindings -A -l app.kubernetes.io/instance={{ .Release.Name }}
